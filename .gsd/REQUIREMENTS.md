# Requirements

This file is the explicit capability and coverage contract for the project.

## Active

### R001 — Single `agent-first-cli` skill replaces the existing `gsd-new-cli-project` and `agent-dx-cli-eval` skills. Old directories removed; new directory at `skills/agent-first-cli/`.
- Class: core-capability
- Status: active
- Description: Single `agent-first-cli` skill replaces the existing `gsd-new-cli-project` and `agent-dx-cli-eval` skills. Old directories removed; new directory at `skills/agent-first-cli/`.
- Why it matters: One install, one mental model. The two skills were always paired (the project skill references the eval); merging eliminates the cross-reference and the "which one do I invoke?" question.
- Source: user
- Primary owning slice: M001/S01
- Validation: mapped
- Notes: Axis tag: none (structural, not content). See S01.

### R002 — SKILL.md advertises 7 user-invocable sub-commands: stack, features, architecture, pitfalls, requirements, validate, verify. Each loads exactly one reference file. SKILL.md itself stays short (progressive disclosure).
- Class: core-capability
- Status: active
- Description: SKILL.md advertises 7 user-invocable sub-commands: stack, features, architecture, pitfalls, requirements, validate, verify. Each loads exactly one reference file. SKILL.md itself stays short (progressive disclosure).
- Why it matters: Sub-commands map 1:1 to GSD's deep-mode research dimensions and to the validate/verify lifecycle. Progressive disclosure keeps the SKILL.md context budget low.
- Source: user
- Primary owning slice: M001/S02
- Validation: mapped
- Notes: Axis tag: none (structural). Sub-commands listed in SKILL.md as a table or list with what each loads.

### R003 — `references/eval.md` exists as the shared 8-axis rubric spine. Not a sub-command. Consumed by validate and verify references.
- Class: core-capability
- Status: active
- Description: `references/eval.md` exists as the shared 8-axis rubric spine. Not a sub-command. Consumed by validate and verify references.
- Why it matters: The 8 axes are the canonical source of truth for "what good looks like." Duplicating them across validate and verify would drift. A shared reference avoids that without making eval a user-facing command.
- Source: user
- Primary owning slice: M001/S02
- Validation: mapped
- Notes: Axis tag: none (this IS the axis definition file). Cross-referenced from validate.md and verify.md.

### R004 — Skill ships `AGENTS.md` (universal agent convention) plus `CLAUDE.md` as a symlink to it. File declares strict bidirectional rule: every axis must trace to an Active R### OR be marked out-of-scope with reason; every R### generated by this skill must trace to an axis OR be justified.
- Class: compliance/security
- Status: active
- Description: Skill ships `AGENTS.md` (universal agent convention) plus `CLAUDE.md` as a symlink to it. File declares strict bidirectional rule: every axis must trace to an Active R### OR be marked out-of-scope with reason; every R### generated by this skill must trace to an axis OR be justified.
- Why it matters: Without enforcement, axis coverage and project requirements drift apart. AGENTS.md is the place agent harnesses read for standing instructions; CLAUDE.md symlink ensures Claude Code picks it up. Strict rule catches both uncovered axes and orphan requirements.
- Source: user
- Primary owning slice: M001/S02
- Validation: mapped
- Notes: Axis tag: none (this IS the enforcement mechanism). Strict = uncovered axis or orphan R### is an error, not a warning.

### R005 — Every reference file in this skill shape-matches its target GSD artifact template: same section headers, same field names, same list formats. Specifically: stack.md → GSD STACK.md prompt shape; features.md → FEATURES.md; architecture.md → ARCHITECTURE.md + CONTEXT.md's Architectural Decisions; pitfalls.md → PITFALLS.md + CONTEXT.md's Risks/Unknowns; requirements.md → per-R### block in GSD's requirements template.
- Class: quality-attribute
- Status: active
- Description: Every reference file in this skill shape-matches its target GSD artifact template: same section headers, same field names, same list formats. Specifically: stack.md → GSD STACK.md prompt shape; features.md → FEATURES.md; architecture.md → ARCHITECTURE.md + CONTEXT.md's Architectural Decisions; pitfalls.md → PITFALLS.md + CONTEXT.md's Risks/Unknowns; requirements.md → per-R### block in GSD's requirements template.
- Why it matters: The closer the match, the more directly GSD's planning agents can lift content out of the reference and drop it into a template without restructuring. Mismatched shapes force the agent to translate, which loses signal and introduces drift.
- Source: user
- Primary owning slice: M001/S03
- Validation: mapped
- Notes: Axis tag: none (meta-quality). Templates being mirrored are at /Users/jason/.gsd/agent/extensions/gsd/templates/ and /Users/jason/.gsd/agent/extensions/gsd/prompts/guided-research-project.md.

### R006 — `references/requirements.md` ships starter R###s in GSD-template format (Class/Status/Description/Why/Source/Owner/Supporting/Validation/Notes). Each starter requirement carries an `Axis: N` tag in its Notes field for traceability.
- Class: core-capability
- Status: active
- Description: `references/requirements.md` ships starter R###s in GSD-template format (Class/Status/Description/Why/Source/Owner/Supporting/Validation/Notes). Each starter requirement carries an `Axis: N` tag in its Notes field for traceability.
- Why it matters: Starter requirements let a user lift a pre-shaped agent-first requirement set into their project's REQUIREMENTS.md without reformatting. The Axis tag enables validate/verify to check coverage without modifying GSD's template.
- Source: user
- Primary owning slice: M001/S03
- Validation: mapped
- Notes: Axis tag: all 8 (this is the bridge between axes and R###s). Coverage table at bottom of file shows axis → starter R###s mapping.

### R007 — `validate` sub-command reads the project's `.gsd/` artifacts (PROJECT.md, REQUIREMENTS.md, ROADMAP.md, CONTEXT.md, PLAN.md) read-only and produces a structured report. Report names each of the 8 axes, its coverage status, and evidence. Uncovered axes and orphan R###s are errors (strict).
- Class: failure-visibility
- Status: active
- Description: `validate` sub-command reads the project's `.gsd/` artifacts (PROJECT.md, REQUIREMENTS.md, ROADMAP.md, CONTEXT.md, PLAN.md) read-only and produces a structured report. Report names each of the 8 axes, its coverage status, and evidence. Uncovered axes and orphan R###s are errors (strict).
- Why it matters: Planning-time validation catches gaps before code is written, when they're cheap to fix. Strict rule means validate fails the build, not just warns.
- Source: user
- Primary owning slice: M001/S04
- Validation: mapped
- Notes: Axis tag: all 8 (validate runs against all axes). Reads eval.md as rubric; never writes to .gsd/.

### R008 — `verify` sub-command reads the actual CLI implementation (commands, formatters, parsers, schemas) read-only and produces a structured report against the same 8-axis rubric. Same strict rule as validate.
- Class: failure-visibility
- Status: active
- Description: `verify` sub-command reads the actual CLI implementation (commands, formatters, parsers, schemas) read-only and produces a structured report against the same 8-axis rubric. Same strict rule as validate.
- Why it matters: Completion-time verification proves the built CLI actually scores 21+ on the rubric, not just that the plan said it would. Catches implementation gaps that planning review missed.
- Source: user
- Primary owning slice: M001/S04
- Validation: mapped
- Notes: Axis tag: all 8. Reads eval.md as rubric; never writes to .gsd/. Uses assets/samples/gitignorer/ as the reference for "what passing looks like."

### R009 — `assets/samples/gitignorer/` ships as a git submodule pointing to `git@github.com:BotTech/gitignore.git`, branch `main`. Not a static copy. The reference implementation (TypeScript/Node/Commander/zod/ndjson) demonstrates all 8 agent-first axes concretely.
- Class: core-capability
- Status: active
- Description: `assets/samples/gitignorer/` ships as a git submodule pointing to `git@github.com:BotTech/gitignore.git`, branch `main`. Not a static copy. The reference implementation (TypeScript/Node/Commander/zod/ndjson) demonstrates all 8 agent-first axes concretely.
- Why it matters: A real, maintained reference beats a contrived one. Submodule keeps the asset fresh as the gitignorer project evolves. Concrete demonstration of each axis anchors validate/verify reports to actual code paths.
- Source: user
- Primary owning slice: M001/S05
- Validation: mapped
- Notes: Axis tag: all 8 (this IS the reference for all axes). Path: assets/samples/gitignorer/. Branch: main.

### R010 — SKILL.md is short (target: under 100 lines). Contains: skill purpose, when-to-use trigger phrases, the 7 sub-command index, progressive-disclosure note explaining that each sub-command loads exactly one reference. No sub-command content lives in SKILL.md itself.
- Class: quality-attribute
- Status: active
- Description: SKILL.md is short (target: under 100 lines). Contains: skill purpose, when-to-use trigger phrases, the 7 sub-command index, progressive-disclosure note explaining that each sub-command loads exactly one reference. No sub-command content lives in SKILL.md itself.
- Why it matters: A short SKILL.md preserves context budget for the actual work. The skill is invoked at multiple points in a project's lifecycle; loading all reference content on every invocation would be wasteful.
- Source: user
- Primary owning slice: M001/S02
- Validation: mapped
- Notes: Axis tag: none (structural). SKILL.md is the entry point; references do the heavy lifting.

### R011 — No file inside the skill ever writes to `.gsd/` in the user's project. The skill is a content provider (reference material) and reader/checker (validate, verify). GSD remains the sole author of `.gsd/` artifacts.
- Class: constraint
- Status: active
- Description: No file inside the skill ever writes to `.gsd/` in the user's project. The skill is a content provider (reference material) and reader/checker (validate, verify). GSD remains the sole author of `.gsd/` artifacts.
- Why it matters: Separation of concerns: GSD owns the artifact lifecycle; the skill owns domain content and validation. Mutating `.gsd/` from within the skill would couple it to GSD's internal format and create write-write conflicts with GSD's own state machine.
- Source: user
- Primary owning slice: M001/S02
- Validation: mapped
- Notes: Axis tag: none (constraint). Applies to all sub-commands and references.

### R012 — No gsd-pi coupling in any skill file. Specifically: no `/gsd` slash command invocations as instruction paths, no `gsd_*` tool calls, no direct reads of `.gsd/gsd.db`, no assumptions about pi-specific extensions being present. Skill works in any agent harness that loads SKILL.md files.
- Class: constraint
- Status: active
- Description: No gsd-pi coupling in any skill file. Specifically: no `/gsd` slash command invocations as instruction paths, no `gsd_*` tool calls, no direct reads of `.gsd/gsd.db`, no assumptions about pi-specific extensions being present. Skill works in any agent harness that loads SKILL.md files.
- Why it matters: The skill targets gsd-core (methodology) not gsd-pi (this harness). Coupling to pi would break the skill in Cursor, Codex, or any non-pi harness. The previous skill's `invoke /gsd-new-project` instruction was exactly this kind of leak.
- Source: user
- Primary owning slice: M001/S02
- Validation: mapped
- Notes: Axis tag: none (constraint). Skills can reference GSD methodology concepts (milestones, slices, requirements) but must not depend on pi's command surface or tool API.

### R013 — Cross-references between reference files use stable axis IDs (e.g., "Axis 1", "Axis 4") rather than relative file paths or section anchors. References to other files use the file's role ("see eval.md", "see references/stack.md") rather than deep links.
- Class: operability
- Status: active
- Description: Cross-references between reference files use stable axis IDs (e.g., "Axis 1", "Axis 4") rather than relative file paths or section anchors. References to other files use the file's role ("see eval.md", "see references/stack.md") rather than deep links.
- Why it matters: Axis IDs survive refactoring of section headers, file moves, and reference rewrites. Deep links break on the first edit; axis IDs are stable.
- Source: user
- Primary owning slice: M001/S03
- Validation: mapped
- Notes: Axis tag: none (meta-quality). Applies across all references.

### R014 — Repo root `README.md` updated: skills index lists `agent-first-cli` (merged) with correct description and trigger phrases; old entries for `gsd-new-cli-project` and `agent-dx-cli-eval` removed. Install instructions unchanged (still `npx skills add https://github.com/bottech/skills`).
- Class: launchability
- Status: active
- Description: Repo root `README.md` updated: skills index lists `agent-first-cli` (merged) with correct description and trigger phrases; old entries for `gsd-new-cli-project` and `agent-dx-cli-eval` removed. Install instructions unchanged (still `npx skills add https://github.com/bottech/skills`).
- Why it matters: README is the first thing a stranger sees. Stale entries for removed skills break trust and cause confusion.
- Source: user
- Primary owning slice: M001/S06
- Validation: mapped
- Notes: Axis tag: none (repo-level). README change is the public marker that the merge shipped.

### R015 — `skills-lock.json` and any symlinks updated to reflect the new skill name. Existing installs that referenced the old skill names get either continued operation (preferred) or a clear migration note in the skill's README.
- Class: launchability
- Status: active
- Description: `skills-lock.json` and any symlinks updated to reflect the new skill name. Existing installs that referenced the old skill names get either continued operation (preferred) or a clear migration note in the skill's README.
- Why it matters: Renaming without a migration path breaks installed users silently. The repo's distribution contract (`npx skills add`) must keep working.
- Source: user
- Primary owning slice: M001/S06
- Validation: mapped
- Notes: Axis tag: none (repo-level). Check skills-lock.json for any references to old paths.

### R016 — `.gitmodules` entry configured for path `assets/samples/gitignorer`, source `git@github.com:BotTech/gitignore.git`, branch `main`. Skill README and repo README document the submodule init step users must run after clone or install (`git submodule update --init --recursive` or `git clone --recurse-submodules`).
- Class: operability
- Status: active
- Description: `.gitmodules` entry configured for path `assets/samples/gitignorer`, source `git@github.com:BotTech/gitignore.git`, branch `main`. Skill README and repo README document the submodule init step users must run after clone or install (`git submodule update --init --recursive` or `git clone --recurse-submodules`).
- Why it matters: A submodule that users don't know how to initialize appears as an empty directory. Silent breakage is worse than no asset at all.
- Source: user
- Primary owning slice: M001/S06
- Validation: mapped
- Notes: Axis tag: none (repo-level). Applies to fresh clones and to `npx skills add` installs.

## Validated

## Deferred

## Out of Scope

### R017 — No scaffolder binary or generator that emits a starter CLI repo. The skill provides content and validation only.
- Class: anti-feature
- Status: out-of-scope
- Description: No scaffolder binary or generator that emits a starter CLI repo. The skill provides content and validation only.
- Why it matters: Out of scope for M001 to keep the milestone focused. A scaffolder is a meaningful separate deliverable that would dilute this milestone's purpose (clarity of the agent-first skill shape). Possible later milestone.
- Source: user
- Notes: Explicit non-goal. Do not add scaffolding features even if easy.

### R018 — No multi-language sample projects. `assets/samples/` contains only `gitignorer` (TypeScript/Node) for v1. No Python/Go/Rust variants.
- Class: anti-feature
- Status: out-of-scope
- Description: No multi-language sample projects. `assets/samples/` contains only `gitignorer` (TypeScript/Node) for v1. No Python/Go/Rust variants.
- Why it matters: Scope discipline. Adding more samples would either dilute maintenance attention or require restructuring the references per-language.
- Source: user
- Notes: One well-documented reference beats four thin ones. Multi-language samples can ship in a later milestone if demand surfaces.

### R019 — No auto-invocation hooks into GSD's planning flow. The skill is explicitly invoked by the user at the right phase, with self-propagation through CONTEXT.md instructions written into the project by GSD itself (not by this skill).
- Class: anti-feature
- Status: out-of-scope
- Description: No auto-invocation hooks into GSD's planning flow. The skill is explicitly invoked by the user at the right phase, with self-propagation through CONTEXT.md instructions written into the project by GSD itself (not by this skill).
- Why it matters: Auto-invocation would require pi-specific hooks, breaking the gsd-core posture. Explicit invocation with self-propagation keeps the skill harness-agnostic.
- Source: user
- Notes: User invokes once at planning; the skill's content (via GSD writing it into CONTEXT.md) ensures it gets re-invoked at validate/verify phases. No skill-side hooks required.

## Traceability

| ID | Class | Status | Primary owner | Supporting | Proof |
|---|---|---|---|---|---|
| R001 | core-capability | active | M001/S01 | none | mapped |
| R002 | core-capability | active | M001/S02 | none | mapped |
| R003 | core-capability | active | M001/S02 | none | mapped |
| R004 | compliance/security | active | M001/S02 | none | mapped |
| R005 | quality-attribute | active | M001/S03 | none | mapped |
| R006 | core-capability | active | M001/S03 | none | mapped |
| R007 | failure-visibility | active | M001/S04 | none | mapped |
| R008 | failure-visibility | active | M001/S04 | none | mapped |
| R009 | core-capability | active | M001/S05 | none | mapped |
| R010 | quality-attribute | active | M001/S02 | none | mapped |
| R011 | constraint | active | M001/S02 | none | mapped |
| R012 | constraint | active | M001/S02 | none | mapped |
| R013 | operability | active | M001/S03 | none | mapped |
| R014 | launchability | active | M001/S06 | none | mapped |
| R015 | launchability | active | M001/S06 | none | mapped |
| R016 | operability | active | M001/S06 | none | mapped |
| R017 | anti-feature | out-of-scope | none | none | unmapped |
| R018 | anti-feature | out-of-scope | none | none | unmapped |
| R019 | anti-feature | out-of-scope | none | none | unmapped |

## Coverage Summary

- Active requirements: 16
- Mapped to slices: 16
- Validated: 0
- Unmapped active requirements: 0
