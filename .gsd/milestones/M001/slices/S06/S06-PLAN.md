# S06: Repo-level updates: README, skills-lock.json, migration note

**Goal:** Update repo-level surfaces (README.md) to reflect the merged agent-first-cli skill: replace the two stale skills-index rows with a single agent-first-cli row, add a migration note for prior gsd-new-cli-project installs, and document the submodule/sample-asset init step. Also audit (and confirm no-op) the skills-lock.json concern by verifying no committed skills-lock.json exists or needs editing.
**Demo:** Repo root README.md skills index lists agent-first-cli with correct description and triggers; old entries removed; skills-lock.json audited (no references to old paths, or migration note added); submodule init step documented in README.

## Must-Haves

- ## Slice-level Success Criteria
- README.md Skills Index contains exactly one row for `agent-first-cli` and zero rows for `agent-dx-cli-eval` or `gsd-new-cli-project`.
- README.md describes the agent-first-cli skill with description and triggers consistent with `skills/agent-first-cli/SKILL.md` frontmatter.
- README.md contains a migration note that names `gsd-new-cli-project` as the prior name and points users at `agent-first-cli`.
- README.md documents the submodule init step (mentions `git submodule update --init` or `git clone --recurse-submodules`, OR provides a standalone clone one-liner for the sample asset).
- No `skills-lock.json` exists anywhere in the committed tree outside `.gsd/` (audit-only finding; no file edit required for this criterion).
- `git submodule status` remains clean (no regression from S01).
- ## Definition of Done
- All tasks complete with verification evidence.
- Single commit on the milestone branch containing the README.md changes.
- Verification battery (`bash`/`grep`) passes per task `verify` commands.
- ## Path convention
- Task paths are recorded in the DB as project-id worktree roots (`/Users/jason/.gsd/projects/0809305c93fd/worktrees/M001/...`) per planner convention. Per MEM011, executors must resolve any absolute worktree path to the current cwd (here: `/Users/jason/src/bottech/skills/.gsd/worktrees/M001/...`). Treat the recorded path as the canonical file identity; the executor resolves it via relative-equivalent (same path suffix from worktree root).

## Proof Level

- This slice proves: contract

## Integration Closure

S06 is a terminal slice. It consumes S02's sub-command names and SKILL.md description for the README row, plus S01's .gitmodules wiring (already in place) for the submodule init docs. No downstream slice consumes S06's output. After S06, the milestone's public-facing surfaces (README) match the merged skill layout, completing the rename's external contract.

## Verification

- Run the task and slice verification checks for this slice.

## Tasks

- [x] **T01: Edit README.md: replace stale skills-index rows, add migration note, document submodule init** `est:30m`
  Why: S06's primary deliverable is the repo-root README reflecting the merged skill. R014 requires the skills index to list agent-first-cli and drop the two old rows; R015's migration concern reduces to a note; R016 requires submodule init docs.
  - Files: `/Users/jason/.gsd/projects/0809305c93fd/worktrees/M001/README.md`
  - Verify: test -f README.md && grep -q 'agent-first-cli' README.md && grep -q 'Updating from before' README.md && grep -q 'git submodule update --init' README.md

- [x] **T02: Audit: verify skills-lock.json requires no S06 edit and submodule wiring remains clean** `est:10m`
  Why: R015's literal text says "audit skills-lock.json". Per S06-RESEARCH Finding 1, skills-lock.json is auto-generated by `npx skills experimental_install`/`experimental_sync`, lives outside the committed BotTech repo, and only tracks third-party skills — never BotTech's own. This task proves the audit was performed and produces the evidence path for S06-SUMMARY, without editing any file.
  - Verify: test -f README.md

## Files Likely Touched

- /Users/jason/.gsd/projects/0809305c93fd/worktrees/M001/README.md
