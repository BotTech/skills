---
estimated_steps: 12
estimated_files: 1
skills_used: []
---

# T02: Author AGENTS.md — strict bidirectional coverage rule, declarative, ≤50 lines

Why: AGENTS.md is the skill's enforcement contract — the smallest file but the hardest to get right. S04's validate.md and verify.md will reference 'the rule defined in AGENTS.md' rather than re-declaring it, so this file must be the single source of truth. Avoid the trap of expanding it into 'everything an agent should know' — that's references/'s job.

Do:
1. Create skills/agent-first-cli/AGENTS.md as plain markdown. NO YAML frontmatter, NO XML tags. AGENTS.md is read directly as a rules file, not parsed as a SKILL.md.
2. Three sections, in this order:
   - Strict Bidirectional Coverage Rule (1–2 short imperative paragraphs): Every axis (1–7 of the agent-first axis rubric in references/eval.md) must map to an Active R### in the project's REQUIREMENTS.md OR be marked out-of-scope with a reason. Every R### generated by this skill (via references/requirements.md) must map to at least one axis OR carry an explicit justification in its Notes field. An uncovered axis or an orphan R### is an ERROR, not a warning. Run agent-first-cli validate (plan-mode) or agent-first-cli verify (impl-mode) to surface errors.
   - Axis → R### direction (one short paragraph or 2–3-row table): how to read coverage top-down. Pointer: see references/eval.md for the canonical axis list; see references/requirements.md for the starter R###s with Axis: N tags.
   - R### → Axis direction (one short paragraph or 2–3-row table): how to read coverage bottom-up. Every R### must carry an Axis: N tag (1–7) in its Notes field, OR an explicit justification if it intentionally does not map to a rubric axis.
3. Do NOT enumerate the 8 axes (those live in references/eval.md, owned by S04). Reference references/eval.md by name only.
4. Do NOT duplicate the validate/verify mechanics (those live in references/validate.md and references/verify.md, owned by S04). AGENTS.md is the contract; validate/verify are the enforcers.
5. Keep total ≤50 lines.
6. Boundary compliance: no instruction to write to .gsd/ in the user's project; no /gsd-*, gsd_*, or gsd.db references.

Done when: file exists at skills/agent-first-cli/AGENTS.md; wc -l reports ≤50; file contains the phrases 'out-of-scope', 'orphan R###', and 'is an error' (strict voice); file references references/eval.md and references/requirements.md by name; no forbidden gsd-pi tokens; no XML tags or YAML frontmatter.

## Inputs

- `/Users/jason/.gsd/projects/0809305c93fd/worktrees/M001/skills/agent-first-cli/SKILL.md`

## Expected Output

- `/Users/jason/.gsd/projects/0809305c93fd/worktrees/M001/skills/agent-first-cli/AGENTS.md`

## Verification

test -f skills/agent-first-cli/AGENTS.md
