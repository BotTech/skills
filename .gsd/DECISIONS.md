# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? | Made By |
|---|------|-------|----------|--------|-----------|------------|---------|
| D001 | M001 discussion | architecture | Skill shape: merge vs keep separate | Merge gsd-new-cli-project and agent-dx-cli-eval into a single agent-first-cli skill with sub-commands and progressive disclosure. | The two existing skills were always paired (same author, same day, cross-referencing each other). Separating them forced users to discover and invoke both, and the project skill's reference to the eval was a fragile cross-directory link. One skill = one install = one mental model. Sub-commands provide the routing surface; progressive disclosure keeps SKILL.md short. | Yes | collaborative |
| D002 | M001 discussion | architecture | Standing-rules file naming convention for this skill | AGENTS.md as canonical rules file; CLAUDE.md as symlink to it | AGENTS.md is the emerging universal convention across agent harnesses (Claude Code, Cursor, Codex, etc.). CLAUDE.md is Claude Code's specific filename. Symlinking CLAUDE.md → AGENTS.md gives Claude Code compatibility without maintaining two files. The strict bidirectional axis↔R### coverage rule lives in AGENTS.md and is read on every invocation. | Yes | collaborative |
| D003 | M001 discussion | architecture | Axis↔R### coverage rule strictness and enforcement | Strict (errors), bidirectional, enforced via validate/verify reports | Soft (warnings only) is easy to ignore and drifts anyway. Strict forces explicit choices: either satisfy the axis or own the decision to skip it (marked out-of-scope with reason). Bidirectional catches both uncovered axes and orphan requirements. AGENTS.md rule is text, but validate/verify sub-commands provide programmatic checks that catch violations regardless of whether the agent enforced the rule at write time. | Yes | collaborative |
| D004 | M001 discussion | architecture | Sample project distribution mechanism | Sample project as git submodule at assets/samples/gitignorer/ | A real, maintained reference beats a contrived one. Static copy would drift the moment gitignorer adds a new axis-relevant feature. Submodule keeps the asset fresh as gitignorer evolves on main. assets/samples/ subdirectory leaves room for future samples without restructuring. Trade-off: requires submodule init step, documented in README. | Yes | collaborative |
| D005 | M001 discussion | architecture | Skill's writing boundary with respect to .gsd/ | No .gsd/ writes anywhere in the skill; content provider + reader/checker only | Separation of concerns: GSD owns the artifact lifecycle and format; the skill owns domain content and validation. Skill-side writes would couple it to GSD's internal format and risk write-write conflicts with GSD's own state machine. The skill provides shape-matched content that GSD lifts into .gsd/ files; the skill reads those files (read-only) during validate and verify. | Yes | collaborative |
| D006 | M001 discussion | architecture | Skill's coupling posture with respect to GSD implementations | Target gsd-core (methodology), not gsd-pi (this harness); no /gsd invocations or gsd_* tool calls in skill files | The skill should work in any agent harness that loads SKILL.md files (Claude Code, Cursor, Codex, pi, etc.). The previous skill's invoke /gsd-new-project instruction was a pi-ism leak that made the skill un-runnable in non-pi harnesses. The skill can reference GSD methodology concepts (milestones, slices, requirements, CONTEXT.md, PLAN.md) because those are gsd-core, but cannot depend on pi's command surface or tool API. | Yes | collaborative |
| D007 | M001 discussion | architecture | Reference file content shape | Every reference file shape-matches its target GSD template (sections, fields, list formats) | The closer the match, the more directly GSD's planning agents can lift content out of the reference and drop it into a template without restructuring. Mismatched shapes force the agent to translate, which loses signal and introduces drift. Concretely: stack.md mirrors guided-research-project.md Task 1 prompt shape; features.md mirrors Task 2; architecture.md mirrors Task 3 + CONTEXT.md Architectural Decisions; pitfalls.md mirrors Task 4 + CONTEXT.md Risks/Unknowns; requirements.md uses the per-R### block from GSD's requirements template. | Yes | collaborative |
| D008 | M001 discussion | architecture | Placement of the 8-axis rubric | eval.md as shared reference (not a sub-command); consumed by validate.md and verify.md | The 8 axes are canonical. Duplicating them across validate and verify would drift. Exposing eval as its own sub-command adds command surface with no clear when-to-invoke trigger (eval is always part of validate or verify, never standalone). Shared reference avoids both problems. | Yes | collaborative |
